{% extends 'base.html.twig' %}
{% block title %}
    Modifier un contact
{% endblock %}

{% block body %}
    <div id="wrapper">

        {{ include('modulesUsed/menu.html.twig') }}

        <div id="page-wrapper">
            <div class="row">
                <div class="col-lg-12">
                    <h1 class="page-header">Modifier un contact</h1>
                </div>
                <!-- /.col-lg-12 -->
            </div>

            <!-- /.row -->
            <div class="row">
                <div class="col-lg-12">
                    {% form_theme edit_form 'bootstrap_3_layout.html.twig' %}
                    {{ form_start(edit_form) }}
                    {{ form_widget(edit_form) }}
                    <div class="row text-center">
                        <div class="btn-group">
                            <input type="submit" value="Modifier"  class="btn btn-success"/>
                            <a href="{{ path('contact_index') }}"  class="btn btn-primary">Retour à la liste</a>
                        </div>

                    </div>
                    {{ form_end(edit_form) }}
                </div>
                <!-- /.col-lg-12 -->
            </div>
        </div>
        <!-- /#page-wrapper -->
    </div>
{% endblock %}