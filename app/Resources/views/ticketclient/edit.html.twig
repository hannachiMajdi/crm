{% extends 'base.html.twig' %}
{% block title %}
    Modifier un Ticket Client
{% endblock %}
{% block body %}
    <div id="wrapper">
        {{ include('modulesUsed/menu.html.twig') }}
        <div id="page-wrapper">
            <div class="row">
                <div class="col-lg-12">
                    <h1 class="page-header"> Modifier un Ticket Client</h1>
                </div>
                <!-- /.col-lg-12 -->
            </div>
            <!-- /.row -->
            <div class="row">
                <div class="col-lg-12">
                    {% form_theme edit_form 'bootstrap_3_layout.html.twig' %}
                    {{ form_start(edit_form) }}
                    {{ form_widget(edit_form) }}
                    <div class=" row text-center">
                        <a href="{{ path('ticketclient_index') }}"  class="btn btn-primary">Retourner à la liste</a>
                        <a href="{{ path('ticketclient_index') }}"  class="btn btn-info">Réassigner à un autre agent</a>
                        <input type="submit" value="Modifier"  class="btn btn-success"/>
                    </div>
                    {{ form_end(edit_form) }}
                </div>
                <!-- /.col-lg-12 -->
            </div>
        </div>
        <!-- /#page-wrapper -->
    </div>
{% endblock %}
