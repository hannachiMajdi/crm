{% extends 'base.html.twig' %}
{% block title %}
    Ajouter un Utilisateur
{% endblock %}

{% block body %}
    <div id="wrapper">

        {{ include('modulesUsed/menu.html.twig') }}

        <div id="page-wrapper">
            <div class="row">
                <div class="col-lg-12">
                    <h1 class="page-header">Ajouter un utilisateur</h1>
                </div>
                <!-- /.col-lg-12 -->
            </div>
            <!-- /.row -->
            <div class="row">
                <div class="col-lg-12">
                    {% form_theme form 'bootstrap_3_layout.html.twig' %}
                    {{ form_start(form) }}
                    {{ form_widget(form) }}
                    <input type="submit" value="Ajouter" class="btn btn-lg btn-success pull-right" />
                    {{ form_end(form) }}


                            <a href="{{ path('contact_index') }}" class="btn btn-lg btn-primary">Retour à la liste</a>

                </div>
                <!-- /.col-lg-12 -->
            </div>
        </div>
        <!-- /#page-wrapper -->
    </div>
{% endblock %}